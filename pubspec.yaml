name: covid19trackerv2
description: A new Flutter application.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^0.1.3
  http: ^0.12.2
  url_launcher: ^5.7.2
  flutter_svg: ^0.17.0
  fl_chart: ^0.11.1
  shared_preferences: ^0.5.12+2
  intl:



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assests/pictures/
    - assests/langs/
  fonts:
    - family: Changa
      fonts:
        - asset: assests/fonts/Changa-VariableFont_wght.ttf

